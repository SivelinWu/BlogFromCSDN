# 原创[自动化测试]Java的反射机制与测试中的应用

> 原文: https://blog.csdn.net/weixin_42390585/article/details/149025105

> *原创内容，未获授权禁止转载、转发、抄袭。
**一、测试需求**  
sdk类产品要对接口的入参增加长度校验，需要逐个测试，参数涉及上百计；手工测试费时费力，考虑开发测试工具
**二、难点**  
参数值比较方便做到参数化，但对象和方法较难实现
**三、解决方案**  
java反射机制
**四、什么是反射机制**  
反射（Reflection），Java 中的反射机制是指，Java 程序在运行期间可以获取到一个对象的全部信息。  
反射机制一般用来解决Java 程序运行期间，对某个实例对象一无所知的情况下，如何调用该对象内部的方法问题。  
![在这里插入图片描述](/cdn/149025105/96607b546b4a4156a7cff01d28e442e6.png)
**1、获取类构造器**  
一旦我们获得了Human的class对象，我们就能够通过这个class对象获得Human类的原始定义信息，首先我们来获取Human类的构造器对象，有了构造器对象我们就能够构造出一个Human对象出来。
    
    Class &lt; Human &gt; humanClass = Human.class;
    try {
        Constructor &lt; Human &gt; c = humanClass.getConstructor(String.class, Integer.class);
        c.setAccessible(true);
        Human human = c.newInstance("男", "21");
        human.eat();
    } catch (Exception e) {
        e.printStackTrace();
    }
    
**2、获取类中定义的方法**  
要获取当前类中定义的方法可以用Class中的getDeclareMethods函数，他会获得当前类中定义的所有的方法（包括public、private、static等方法），它会返回一个Method对象数组，其中每一个Method的对象即表示类中定义的一个方法。要想获得类中定义的指定的方法可以用getDeclareMethod(Stirng name,Class…parameterType)。
    
    try {
        Class &lt; Human &gt; humanClass = Human.class;
        Human human = humanClass.newInstance();
        //初始化对象                
        Method[] methods = humanClass.getDeclaredMethods();
        for (Method method: methods) {
            System.out.println("方法名：" + method.getName());
        }
        //获得指定的方法                        
        Method eat = humanClass.getDeclaredMethod("eat", String.class);
        if (Modifier.isPrivate(eat.getModifiers())) {
            //判断eat()的作用域是否为私有                                
            System.out.println("private");
        }
        eat.invoke(human);
        //调用eat()方法                
    } catch (Exception e) {
        e.printStackTrace();
    }
    
3、获取类中定义的属性  
获取属性和获取方法类似，只不过将getMethod()换成了getField()，getDeclaredMethod()换成了getDeclareField()。
    
    try {
        Class &lt; Human &gt; humanClass = Human.class;
        Field[] fields = humanClass.getDeclaredFields();
        for (Field field: fields) {
            System.out.println("属性名：" + field.getName());
        }
        //获得指定的属性                
        Field sex = humanClass.getDeclaredField("sex");
    	} catch (Exception e) {
        	e.printStackTrace();
    	}
    }
    
4、获取父类和接口  
我们先定义一个子类Man去继承Human类，如下：
    
    class Man extends Human {
        private String beard;
        //有胡子        
        @Override public void eat() {
            //吃饭行为           
            System.out.println("只吃午餐");
        }
    }
    
最后通过class对象的getSuperClass()得到man的父类，通过getInterFaces获取接口。
    
    Class &lt; Man &gt; manClass = Man.class;
    Class &lt;? &gt; humanClass = manClass.getSuperclass();
    Class &lt;? &gt; [] interfaces = manClass.getInterfaces();
    for (Class &lt;? &gt; i: interfaces) {
        System.out.println(i.getName());
    }
    
**五、如何应用**  
入参文件（用例）：  
![在这里插入图片描述](/cdn/149025105/8b90a7e28ecf4f37bf7119f41edf5f63.png)  
通过获取入参文件中的方法名，利用反射机制，调用方法用例执行：  
![在这里插入图片描述](/cdn/149025105/52b49933851f4ad4acb388c49fad9465.png)
